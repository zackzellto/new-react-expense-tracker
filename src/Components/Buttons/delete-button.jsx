import { render } from "@testing-library/react";
import React from "react";
import { Button } from "react-bootstrap";

const submitButton = () => {
        return(
            <Submit>
                Submit!
            </Submit>
        )
    }
